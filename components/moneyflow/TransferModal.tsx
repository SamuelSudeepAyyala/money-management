import { FormEvent } from "react";
import { Account } from "./types";
import { CalendarInput } from "./CalendarInput";
export function TransferModal({ accounts, onClose, onSubmit }: { accounts: Account[]; onClose: () => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void }) {
  return <div className="modal-backdrop" onClick={onClose}><form className="modal" onSubmit={onSubmit} onClick={event => event.stopPropagation()}><div className="modal-heading"><div><p className="eyebrow">ACCOUNT TRANSFER</p><h2>Move money</h2><p className="form-hint">Transfer between accounts you own.</p></div><button type="button" className="close-button" onClick={onClose}>×</button></div><label>From account<select name="from_account_id" defaultValue={accounts[0]?.id} required>{accounts.map(account => <option key={account.id} value={account.id}>{account.name}</option>)}</select></label><label>To account<select name="to_account_id" defaultValue={accounts[1]?.id || accounts[0]?.id} required>{accounts.map(account => <option key={account.id} value={account.id}>{account.name}</option>)}</select></label><label>Amount<input name="amount" type="number" min="0.01" step="0.01" required /></label><label>Transfer date<CalendarInput name="occurred_on" required /></label><label>Note<input name="note" placeholder="Optional note" /></label><button className="primary-button full" type="submit">Transfer money</button></form></div>;
}