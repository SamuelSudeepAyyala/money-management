import { useMemo } from "react";
import { ApiRecurringBill } from "../../services/api";
import { currency } from "./format";
import { Legend } from "./Legend";
import { Transactions } from "./Transactions";
import { Upcoming } from "./Upcoming";
import { Transaction } from "./types";

type OverviewProps = { transactions: Transaction[]; balance: number; recurringBills: ApiRecurringBill[]; onViewTransactions: () => void; onViewRecurringBills: () => void; onNotice: (message: string) => void };

export function Overview({ transactions, balance, recurringBills, onViewTransactions, onViewRecurringBills, onNotice }: OverviewProps) {
  const totals = useMemo(() => ({ income: transactions.filter(transaction => transaction.type === "income").reduce((sum, transaction) => sum + transaction.amount, 0), spending: transactions.filter(transaction => transaction.type === "expense").reduce((sum, transaction) => sum + transaction.amount, 0) }), [transactions]);
  const rate = totals.income ? Math.max(0, Math.round((1 - totals.spending / totals.income) * 100)) : 0;
  const categories = ["Housing", "Food & groceries", "Subscriptions", "Other"];
  const colors = ["#5874e8", "#e5ad54", "#8b78d5", "#b9c5f4"];
  return <><div className="stats-grid"><article className="stat-card balance-card"><div className="stat-label">TOTAL BALANCE <button type="button" className="help help-button" onClick={() => onNotice("Total balance combines your account opening balances and transactions.")}>?</button></div><div className="stat-value">{currency(balance)}</div><div className="stat-foot"><span>Live from your saved accounts</span></div><div className="sparkline"><span /><span /><span /><span /><span /><span /><span /><span /><span /><span /></div></article><article className="stat-card"><div className="stat-label">INCOME <button type="button" className="period period-button" onClick={() => onNotice("Income is calculated from your saved transactions.")}>THIS MONTH⌄</button></div><div className="stat-value">{currency(totals.income)}</div><div className="stat-foot positive">↗ Updated live <span>from transactions</span></div></article><article className="stat-card"><div className="stat-label">SPENDING <button type="button" className="period period-button" onClick={() => onNotice("Spending is calculated from your saved transactions.")}>THIS MONTH⌄</button></div><div className="stat-value">{currency(totals.spending)}</div><div className="stat-foot negative">↘ Tracked live <span>from transactions</span></div></article><article className="stat-card"><div className="stat-label">SAVINGS RATE <button type="button" className="help help-button" onClick={() => onNotice("Savings rate is income minus spending divided by income.")}>?</button></div><div className="stat-value">{rate}<span className="cents">%</span></div><div className="progress"><span style={{ width: `${rate}%` }} /></div><div className="stat-foot">Calculated from entries</div></article></div><div className="section-grid"><section className="panel spending-panel"><div className="panel-heading"><div><h2>Spending overview</h2><p>Where your money went this month</p></div><button type="button" className="select-button" onClick={() => onNotice("Month filters will be available in the reporting milestone.")}>This month⌄</button></div><div className="chart-area"><div className="donut"><div className="donut-center"><strong>{currency(totals.spending)}</strong><small>total spent</small></div></div><div className="legend">{categories.map((category, index) => { const amount = transactions.filter(transaction => transaction.type === "expense" && (index === categories.length - 1 ? !categories.slice(0, -1).includes(transaction.category) : transaction.category === category)).reduce((sum, transaction) => sum + transaction.amount, 0); return <Legend key={category} color={colors[index]} label={category} amount={currency(amount)} percent={totals.spending ? `${((amount / totals.spending) * 100).toFixed(1)}%` : "0%"} />; })}</div></div></section><section className="panel upcoming-panel"><div className="panel-heading"><div><h2>Upcoming</h2><p>Payments due soon</p></div><button type="button" className="text-button" onClick={onViewRecurringBills}>View all →</button></div>{recurringBills.length ? recurringBills.slice(0, 3).map((bill, index) => <Upcoming key={bill.id} icon="↻" title={bill.name} meta={`${bill.frequency} · Due ${bill.next_due}`} amount={currency(bill.amount)} color={["blue", "purple", "gold"][index] as "blue" | "purple" | "gold"} />) : <div className="empty-state">No upcoming bills yet.</div>}</section></div><Transactions transactions={transactions.slice(0, 5)} compact onViewAll={onViewTransactions} /></>;
}
