import { FormEvent, useState } from "react";

export function AuthScreen({ error, loading, onLogin, onRegister }: { error: string; loading: boolean; onLogin: (email: string, password: string) => void; onRegister: (email: string, password: string, displayName: string) => void }) {
  const [registering, setRegistering] = useState(false);
  const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const data = new FormData(event.currentTarget); const email = String(data.get("email")); const password = String(data.get("password")); registering ? onRegister(email, password, String(data.get("displayName"))) : onLogin(email, password); };
  return <main className="auth-shell"><section className="auth-card"><div className="brand auth-brand"><span className="brand-mark">✦</span><span>Money<span className="brand-accent">Flow</span></span></div><p className="eyebrow">PRIVATE FINANCE WORKSPACE</p><h1>{registering ? "Create your account" : "Welcome back"}</h1><p className="auth-subtitle">{registering ? "Start organizing your money in one secure place." : "Sign in to continue to your finances."}</p><form onSubmit={submit}>{registering && <label>Name<input name="displayName" placeholder="Your name" required /></label>}<label>Email<input name="email" type="email" placeholder="you@example.com" required /></label><label>Password<input name="password" type="password" minLength={12} placeholder="At least 12 characters" required /></label>{error && <p className="auth-error" role="alert">{error}</p>}<button className="primary-button full" disabled={loading}>{loading ? "Please wait…" : registering ? "Create account" : "Sign in"}</button></form><button className="auth-switch" onClick={() => setRegistering(value => !value)}>{registering ? "Already have an account? Sign in" : "Need an account? Create one"}</button><small className="auth-note">Passwords are hashed by the backend and never stored directly.</small></section></main>;
}
