import { Account } from "./types";

export function AddTransactionModal({ accounts, onClose, onSubmit }: { accounts: Account[]; onClose: () => void; onSubmit: (event: React.FormEvent<HTMLFormElement>) => void }) {
  return <div className="modal-backdrop" onClick={onClose}><form className="modal" onSubmit={onSubmit} onClick={event => event.stopPropagation()}><div className="modal-heading"><div><p className="eyebrow">QUICK ENTRY</p><h2>Add transaction</h2></div><button type="button" className="close-button" onClick={onClose}>×</button></div><label>Type<select name="type" defaultValue="expense"><option value="expense">Expense</option><option value="income">Income</option></select></label><label>Name<input name="name" placeholder="e.g. Coffee shop" required /></label><label>Amount<input name="amount" type="number" min="0.01" step="0.01" placeholder="0.00" required /></label><label>Category<select name="category" defaultValue="Other"><option>Housing</option><option>Food & groceries</option><option>Transportation</option><option>Subscriptions</option><option>Other</option></select></label><label>Account<select name="account" defaultValue={accounts[0]?.name}>{accounts.map(account => <option key={account.id}>{account.name}</option>)}</select></label><label>Notes<input name="notes" placeholder="Optional note" /></label><button className="primary-button full" type="submit">Save transaction</button></form></div>;
}

